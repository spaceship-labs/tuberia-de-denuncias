'use strict';

describe('Controller: CalificaEmptyCtrl', function () {

  // load the controller's module
  beforeEach(module('tuberiaPrototypeApp'));

  /*
  var CalificaEmptyCtrl,
    scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    CalificaEmptyCtrl = $controller('CalificaEmptyCtrl', {
      $scope: scope
      // place here mocked dependencies
    });
  }));

  it('should attach a list of awesomeThings to the scope', function () {
    expect(CalificaEmptyCtrl.awesomeThings.length).toBe(3);
  });
  */
});
