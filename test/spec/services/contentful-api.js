'use strict';

describe('Service: contentfulApi', function () {

  // load the service's module
  beforeEach(module('tuberiaPrototypeApp'));

  // instantiate service
  /*
  var contentfulApi;
  beforeEach(inject(function (_contentfulApi_) {
    contentfulApi = _contentfulApi_;
  }));

  it('should do something', function () {
    expect(!!contentfulApi).toBe(true);
  });

  */
});
