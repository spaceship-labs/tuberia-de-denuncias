'use strict';

describe('Service: schoolsService', function () {

  // load the service's module
  beforeEach(module('tuberiaPrototypeApp'));

  // instantiate service
  var schoolsService;
  beforeEach(inject(function (_schoolsService_) {
    schoolsService = _schoolsService_;
  }));

  /*it('should do something', function () {
    expect(!!schoolsService).toBe(true);
  });*/

});
