'use strict';

describe('Directive: ga', function () {

  // load the directive's module
  beforeEach(module('tuberiaPrototypeApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
});
